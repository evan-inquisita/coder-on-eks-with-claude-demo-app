FROM node:20-alpine
WORKDIR /app

# Install deps separately so they're cached across source edits
COPY package.json package-lock.json* ./
RUN npm install

EXPOSE 3000
CMD ["npm", "run", "dev", "--", "--host"]
